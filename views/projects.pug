extends layout

block content
  div
    h1 List of project proposals
    for project in projects
      div
        h2= project.project_name
block li_signout
  if(user)
    li
      a(href="/sign-out") Sign out
block li_signin
  if(!user)
    li
      a(href="/signin") Sign in